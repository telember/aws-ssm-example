# Local .terraform directories (contains provider binaries and modules)
**/.terraform/*

# Terraform state files (may contain sensitive information)
*.tfstate
*.tfstate.*

# Crash log files generated by Terraform
crash.log
crash.*.log

# Variable files (often contain secrets like passwords or keys)
*.tfvars
*.tfvars.json

# Override files (used to override resources locally, not meant for version control)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform CLI configuration files
.terraformrc
terraform.rc
.terraform.lock.hcl
